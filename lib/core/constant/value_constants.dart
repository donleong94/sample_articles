class ValueConstants {
  ValueConstants._();

  // Common
  static const String defaultLangCode = "en";
  static const String defaultLangCountryCode = "US";

  // Dio client instance name
  static const String articleClient = "articleClient";
}
